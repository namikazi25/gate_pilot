# ── Large / regenerable artifacts ───────────────────────────────────────────
# Checkpoints are archived to the private HF repo namikazii25/gate-pilot-checkpoints
# (restore: hf download namikazii25/gate-pilot-checkpoints --repo-type model --local-dir ckpt)
ckpt/
# Activation/weight-diff vectors — regenerated by src/gate_corpus2.py & src/grow_installs.py
out/vectors/

# ── Python ──────────────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*.egg-info/
.ipynb_checkpoints/
.venv/
venv/
env/

# ── Hugging Face / upload state ─────────────────────────────────────────────
.cache/
**/.cache/huggingface/
.hf_upload.log

# ── LaTeX build artifacts (the .pdf, .tex, and figures stay tracked) ─────────
writeup/*.aux
writeup/*.log
writeup/*.out
writeup/*.toc
writeup/*.fls
writeup/*.fdb_latexmk
writeup/*.synctex.gz
writeup/*.bbl
writeup/*.blg

# ── OS / editor ─────────────────────────────────────────────────────────────
.DS_Store
*.swp
*~
